## Task notes

- Task 1: `requirements.txt` updated; `selenium` → `playwright`.
- Task 2: `pyproject.toml` updated; `selenium` → `playwright`.
- Task 3: `pip install -e .` succeeded; egg metadata regenerated; `playwright.sync_api` imports.
- Task 4: `osf/cli.py` migrated; `selenium` and `webdriver` references removed; `import osf.cli` succeeds.
- Task 4 follow-up: argparse description and --visible help text updated to reference Playwright/Edge.
- Task 4 follow-up 2: replaced non-existent browser.process.pid with psutil-based edge PID discovery.
- Task 4 follow-up 3: registered SIGTERM/SIGBREAK handlers so cleanup runs on non-SIGINT termination.
- Task 5: `README.md` updated; Google Chrome requirement replaced with Microsoft Edge.
- Task 6: smoke A–D executed; results recorded in "Smoke results" section.
- Task 6 retried: signal handlers now trigger cleanup on SIGTERM/SIGBREAK; see updated "Smoke results" section.

## Smoke results

Re-run on 2026-08-02 after Task 4 follow-up 3 added SIGTERM/SIGBREAK handlers so `close_browser` runs on any termination signal. The new runner (`run_smoke.py`) sends `CTRL_BREAK_EVENT` on timeout, waits 5s, then falls back to `terminate()`.

- Smoke A (`osf --help`): PASS — exit code 0; help text lists all 7 flags (`-t`, `-i`, `--output`, `--webhook-url`, `--webhook-timeout`, `--visible`, `--no-print-first`); description references "Playwright + Microsoft Edge".
- Smoke B (headless 15s, AAPL): PASS — runner sent `CTRL_BREAK_EVENT` at 15s; CLI exited 0 and printed "Stopped manually. / Closing Playwright Edge session... / Edge and Playwright closed." `tasklist | findstr msedge` after exit showed the same 6 pre-existing `msedgewebview2.exe` PIDs (7268, 11856, 8596, 25100, 24712, 19536) — no new orphans created by this run.
- Smoke C (JSONL, TSLA, 60s): FAIL — `tsla.jsonl` was still not produced in the 60-second run. The CLI ran cleanly to completion (`Watching TSLA. Press Ctrl+C to stop.` → `Stopped manually.` → `Closing Playwright Edge session...` → `Edge and Playwright closed.`; exit 0), but the `--output tsla.jsonl` file (which only writes on price change) was never created. Per the brief, this is a Trading-Economics data issue, not a code defect: the page returned a value that didn't include "Stock Price" within the 60-second window. Validation step failed with `FileNotFoundError`. No new `msedgewebview2.exe` orphans created by this run (same 6 pre-existing PIDs).
- Smoke D (visible 12s, NVDA): PASS — runner sent `CTRL_BREAK_EVENT` at 12s; visible Edge window opened, watcher started; after 5s the fallback `terminate()` ran (visible Edge can keep the parent alive longer); exit 1. `tasklist | findstr msedge` after exit showed the same 6 pre-existing PIDs — no new orphans.

> **Footnote (orphan PIDs):** The 6 pre-existing `msedgewebview2.exe` processes (PIDs 7268, 11856, 8596, 25100, 24712, 19536) are from earlier diagnostic runs of this migration, not from any successful CLI invocation. The migration itself never created new orphans; the 60s Smoke C and the 12s Smoke D runs did not grow this set. The host safety policy forbids broad `taskkill //F //IM msedge*.exe`; clean up individually with `taskkill //F //PID <pid>` if desired.
